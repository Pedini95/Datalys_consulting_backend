---
name: Deploy 

on:
  push:
    branches:
        - main
        - master
        - develop
        - staging

        
env:
  APP_PORT: 5000

jobs:
  deploy:
    uses: OrangeLiberia/pipelines/.github/workflows/deploy.yml@main
